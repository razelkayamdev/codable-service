{
  "name": "codable-service",
  "version": "1.0.0",
  "description": "a micro service that encodes and decodes messages",
  "main": "Launcher.ts",
  "scripts": {
    "build" : "rm -rf dist && tsc -p .",
    "start": "node dist/Launcher.js",
    "start:prod": "npm install && npm run build && npm run start",
    "start:dev": "npm install && ts-node src/Launcher.ts",
    "test": "jest"
  },
  "author": "Raz Elkayam",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.1",
    "@types/jest": "^27.4.0",
    "dotenv": "^10.0.0",
    "jest": "^27.4.7",
    "ts-jest": "^27.1.3",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.5"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
